Ext.define('PriceCom.model.Tienda', {
    extend: 'Ext.data.Model',

    config: {
        fields: [
            'nombre',
            'direccion',
            'longitud',
            'latitud'
        ]
    }
});